import React from 'react';

const ErrorPage = () => (
    <div className="error-layout">
        <h1 className="error-text">An error encountered while fetching, please try again later!</h1>
    </div>
);

export default ErrorPage;